"use client";
import {FormEvent,useState} from "react";
const employees=["Cassidy Phillips","Tristin Vogel","Natalya Garrod","Connie Beneteau"];
export default function LoginForm(){
 const [role,setRole]=useState<"employee"|"admin">("employee"),[name,setName]=useState(employees[0]),[email,setEmail]=useState(""),[password,setPassword]=useState("");
 const submit=(event:FormEvent)=>{event.preventDefault();const displayName=role==="admin"?"Julia Bernoski":name;window.location.assign(`/portal?role=${role}&name=${encodeURIComponent(displayName)}&email=${encodeURIComponent(email)}`)};
 return <main className="login-page"><section className="login-story" aria-label="KLB Time and Expense Portal introduction"><div className="login-brand"><span>K</span><b>KLB Portal</b><small>Time & expenses</small></div><div className="login-copy"><small>YOUR WORK, ORGANIZED</small><h1>One place for time, travel, and expenses.</h1><p>Submit weekly hours, mileage, and expenses with a clear view of every pay period.</p><ul><li><i>✓</i> Private employee records</li><li><i>✓</i> Admin-only team oversight</li><li><i>✓</i> Simple biweekly reporting</li></ul></div><p className="login-foot">Designed for a simple and accessible payroll experience.</p></section><section className="login-panel"><form className="login-card" onSubmit={submit}><span className="login-icon" aria-hidden="true">↗</span><small>WELCOME BACK</small><h2>Sign in to KLB</h2><p>Select your account type and enter your portal details.</p><div className="login-tabs" aria-label="Account type"><button type="button" className={role==="employee"?"active":""} onClick={()=>setRole("employee")}>Employee</button><button type="button" className={role==="admin"?"active":""} onClick={()=>setRole("admin")}>Administrator</button></div>{role==="employee"&&<label>Employee<select value={name} onChange={e=>setName(e.target.value)}>{employees.map(person=><option key={person}>{person}</option>)}</select></label>}<label>Email address<input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} placeholder={role==="admin"?"julia@klbconsultants.ca":"name@klbconsultants.ca"} required/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/></label><div className="login-options"><label><input type="checkbox"/> Remember me</label><button type="button">Forgot password?</button></div><button className="login-button" type="submit">Sign in as {role==="admin"?"administrator":"employee"} <span>→</span></button><p className="demo-note"><b>Prototype login</b> Use any email and password to explore the portal. No account information is stored.</p></form></section></main>
}
